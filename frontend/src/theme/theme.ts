import { designTokens } from "@/styles/designTokens";

const { colors } = designTokens;

export const THEME = {
  bg: colors.background,
  panel: colors.panel,
  panelSecondary: colors.backgroundSecondary,
  panelHover: colors.panelHover,
  border: colors.border,
  borderStrong: colors.borderStrong,
  text: colors.textPrimary,
  textSecondary: colors.textSecondary,
  mutedText: colors.mutedText,
  accent: colors.accent,
  accentSoft: colors.accentSoft,
  green: colors.bullish,
  red: colors.bearish,
  yellow: colors.warning,
  positiveTagBg: colors.positiveTagBg,
  negativeTagBg: colors.negativeTagBg,
  panelInset: colors.panelInset,
  overlay: "rgba(30, 34, 41, 0.92)",
};
